# ══════════════════════════════════════════════════════════════════════
# AgentFi Root — .env
#
# NOTE: The main config is in agents/.env — see agents/.env.example
# This root file is ONLY needed for contract deployment (Foundry).
# The frontend works without any .env (all values have hardcoded fallbacks).
# ══════════════════════════════════════════════════════════════════════

# ─── Contract Deployment (Foundry — only if deploying new contracts) ──
DEPLOYER_PRIVATE_KEY=0x...

# ─── Hedera (shared with agents/.env for Foundry scripts) ──────────
HEDERA_ACCOUNT_ID=0.0.7973940
HEDERA_PRIVATE_KEY=

# ─── Anthropic (shared with agents/.env) ───────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
