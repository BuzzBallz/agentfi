// TODO: Replace stub with useReadContract from wagmi to fetch agent data from AgentNFT contract

export function useAgentData(_agentId: number) {
  return {
    data: undefined,
    isLoading: false,
    isError: false,
  };
}
