// TODO: Replace stub with useReadContract from wagmi to fetch listed agents from AgentMarketplace

export function useListedAgents() {
  return {
    agents: [] as unknown[],
    isLoading: false,
    isError: false,
  };
}
